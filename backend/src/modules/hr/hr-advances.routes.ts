import { getCookie } from 'hono/cookie'; import { Hono, type Context } from 'hono'; import { prisma } from '../../lib/prisma.js'; import { getCurrentUser } from '../auth/auth.service.js'; import { z } from 'zod';
async function currentUser(c: Context) { try { return await getCurrentUser(getCookie(c, 'workpilot_access') ?? ''); } catch { return null; } }
const schema=z.object({status:z.enum(['approved','rejected'])});
export const hrAdvancesRoutes=new Hono().get('/',async(c)=>{const user=await currentUser(c);if(!user||!['hr','admin'].includes(user.role))return c.json({message:'Unauthorized.'},403);const requests=await prisma.salaryAdvanceRequest.findMany({orderBy:{createdAt:'desc'},include:{user:{select:{employeeId:true,fullName:true}}}});return c.json({requests});}).patch('/:id/status',async(c)=>{const user=await currentUser(c);if(!user||!['hr','admin'].includes(user.role))return c.json({message:'Unauthorized.'},403);const data=schema.safeParse(await c.req.json().catch(()=>null));if(!data.success)return c.json({message:'Invalid status.'},400);const request=await prisma.salaryAdvanceRequest.update({where:{id:c.req.param('id')},data:{status:data.data.status,reviewedAt:new Date()}}).catch(()=>null);return request?c.json({request}):c.json({message:'Request not found.'},404);});
